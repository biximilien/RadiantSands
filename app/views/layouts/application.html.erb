<!DOCTYPE html>
<html>
  <head>
    <title>RadiantSands</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div id="page-content" class="container-fluid">
      <%= render 'layouts/navbar' %>

      <section id="page-body" class="col-md-6 col-md-offset-3 well" style="margin-top: 15px; margin-bottom: 15px;">
        <%= yield %>
      </section>

      <div class="col-md-3">
        <section id="advertisement">
          <% 
             # link_to( image_tag("http://localhost:3000#{@ad.image.url}"), '#',
             # id: "ad-#{@ad.id}", class: 'ad',
             #  data: { 'image-url' => @ad.image }
             # ) unless @ad.nil?
          %>
        </section>
      </div>

    </div>

  </body>
</html>

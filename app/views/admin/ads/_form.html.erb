<%= form_for(@admin_ad) do |f| %>
  <% if @admin_ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_ad.errors.count, "error") %> prohibited this admin_ad from being saved:</h2>

      <ul>
      <% @admin_ad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

$('#events').append("<%= j render 'events/event' %>");

<% if events.next_page %>
  $('.pagination').replaceWith('<%= j paginate events %>');

<% else %>
  $(window).off('scroll');
  $('.pagination').remove();

<% end %>